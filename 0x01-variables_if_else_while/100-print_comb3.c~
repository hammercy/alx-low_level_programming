#include <stdio.h>

/**
 * main - Print the combination of two digits
 *Return: Always 0 (success)
 */
int main(void)
{
	int i;
	int j;

	for (i = 48; i < 57; i++)
	{
		for  (j = i + 1; j < 58; j++)
		{
			putchar(i);
			putchar(j);
			if (i != 8  && j != 9)
			{
				putchar(',');
				putchar(' ');
			}
			else
			{
				putchar('\n');
			}
		}
	}
	return (0);
}
