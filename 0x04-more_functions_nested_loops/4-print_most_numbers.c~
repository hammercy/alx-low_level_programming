#include <stdio.h.
#include "main.h"

/**
 * print_most_numbers - print 1-9 number without 2 and 4
 *
 */
void print_most)numbers(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		if (i == 2 || i == 4)
			continue;
		else
			_putchar(i + 48);
}
